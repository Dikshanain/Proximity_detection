# .env
PORT=8080
# Default radius if client doesn't set one (in kilometers)
DEFAULT_RADIUS_KM=30

# How long (seconds) to keep a user without updates before pruning
PRESENCE_TTL=600
# CORS allowed origins (comma separated). Use "*" for local/dev.
CORS_ORIGINS=*
